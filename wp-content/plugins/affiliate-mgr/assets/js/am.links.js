(function(global) {
    // Namespace
    var am = global.am || (global.am = {});
    am.dashboard = (function(){
        var context = {};

        //placeholder for links-specific javascript

        return context;
    })();
})(this);